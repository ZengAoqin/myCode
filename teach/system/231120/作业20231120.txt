1.编写一个程序，利用多进程以及无名管道，实现下述命令的执行效果：ls | wc -w

2.编程实现两个程序：一个代表服务器，一个代表客户端。要求
A)服务器采用守护进程（精灵进程）的方式在后台运行，
常驻内存。服务器创建并监视有名管道FIFO。
一旦发现有数据，服务器将其保存到一个指定的地方

B)客户端每隔一段时间产生一个子进程，
这些子进程将当前系统时间和自身的PID写入有名管道FIFO就退出。