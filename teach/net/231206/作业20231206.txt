1.修改tcp文件传输，同时发送文件名和文件大小，顺便测试看看程序
2.编写一个聊天程序套件，包含一个服务器程序，以及一个客户端程序，服务器在后台持续运行，负责接收不同主机的客户端
发来的连接请求
接收客户端发送的消息
提示：将连接套接字和对端地址存储打包成一个节点，管理一个客户端的连接信息
将连接成功的客户端存放到结构体数组对象中，给每个客户端对象创建一个线程获取消息，将子线程设置为分离属性自己回收数据

